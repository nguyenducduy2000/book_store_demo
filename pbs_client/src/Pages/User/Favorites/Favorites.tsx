import { Layout } from 'antd';

const { Content } = Layout;

function Favorites() {
    return (
        <Layout>
            <Content>
                <h1>Favorites</h1>
            </Content>
        </Layout>
    );
}

export default Favorites;
